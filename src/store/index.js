import { createStore } from 'vuex'
import albumsModule from './modules/albums';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    albumsModule,
  }
})
